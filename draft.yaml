#modelos, database, factories
models:
  #tablas unilaterales
  Empresa:
    codigo: integer
    periodo: date
    razon_social: string
    razon_comercial: string
    ruc: string
    direccion: string
    telefono: string
    email: email unique
    email_password: string
    email_server: string
    email_port: string
    email_security: string
    obligado_contabilidad: boolean
    contador_nombre: string nullable
    contador_ruc: string nullable
    representante_nombre: string nullable
    representante_ruc: string nullable
    periodo_inicio: date nullable
    periodo_fin: date nullable
    tipo_cuentas: string
    logotipo: string nullable
    firma_electronica: string nullable
    firma_electronica_password: string nullable
    firma_electronica_expiracion: date nullable
    firma_electronica_actualizacion: date nullable
    tipo_emision: boolean
    ambiente: boolean
    facturar_negativo: boolean
    moneda_id: id
    provincia_id: id
    ciudad_id: id
    email_facturacion: email nullable
    leyenda_facturacion: string nullable
    relationships:
        hasMany: Establecimiento, Punto_emision

  Establecimiento:
    codigo: integer
    nombre: string
    url_web: string
    nombre_comercial: string
    direccion: string
    estado: boolean
    empresa_id: id
    relationships:
        hasMany: Punto_emision

  Punto_emision:
    codigo: integer
    nombre: string
    emision: string
    factura: string
    nota_credito: string
    nota_debito: string
    guia_remision: string
    retencion_compra: string
    liquidacion_compra: string
    estado: boolean
    empresa_id: id
    establecimiento_id: id


  #tablas globales
  Ciudad:
    codigo: string
    nombre: string
    provincia_id: id
  Provincia:
    codigo: string
    nombre: string
    relationships:
      hasMany: Ciudad
  Moneda:
    nombre: string
    valor: decimal:14,6 default:0

